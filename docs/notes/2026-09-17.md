# 2026-09-17 개발일지

## 저장소 안내 정리

- 기준 저장소 `trading-AI`와 빈 저장소 `trading_AI`를 구분했다.
- README에 현재 운용 실행, 새 PC의 원본·모델 준비 조건, 모델 검증 한계를 명시했다.
- 실제로 없는 `scripts/` 경로와 다른 PC의 절대 경로를 pipeline 문서에서 제거했다.
- architecture 문서에서 구현된 운용과 향후 뉴스·시장 국면 설계를 구분했다.
- repository guide를 추가하고 기존 메모·초기 실험·산출물은 보존했다.
- 새 로컬 산출물·환경 파일을 제외하고 직접 의존성 `ccxt`, `streamlit`을 복구했다.

모델 학습, 전체 백테스트, 대형 데이터 생성, Git history 변경은 하지 않았다.
기존 모델 재검증, F04/F05 accounting 수정, 공통 평가 지표는 여전히 남아 있다.

## 검증

- 기존 synthetic 회귀 테스트 17개 통과
- README와 새 구조 문서의 상대 링크 확인 통과
- 운용 CSV·모델·PID·환경 파일 제외 설정 확인 통과
- `git diff --check` 통과
