# AGENTS.md

이 저장소에서 Codex가 작업할 때 지켜야 할 핵심 규칙이다.

## 프로젝트 정체성

- 이 프로젝트는 가격을 정확히 맞히는 AI가 아니다.
- 목표는 `BTC Market Regime & Risk Guard AI`를 만드는 것이다.
- 가격 방향성 예측은 보조 신호이며, 핵심은 시장 국면, 고변동 위험, 급락 위험, 이벤트 리스크를 감지해 리스크 관리를 돕는 것이다.

## 작업 원칙

- 기존 `data/`, `model/`, `models/`, `experiments/`의 대용량 산출물은 삭제하거나 덮어쓰지 않는다.
- 기존 메모와 기록 문서는 보존하고, 정리가 필요하면 새 문서로 재구성한다.
- 소스코드를 수정하기 전에는 관련 문서와 현재 파이프라인을 먼저 확인한다.
- time-series 데이터는 시간 순서를 지키며 다루고, `shuffle` 기반 검증을 기본값으로 사용하지 않는다.
- lookahead bias가 생길 수 있는 피처, 타깃, 백테스트 로직을 특히 조심한다.
- 사용자가 명시적으로 요청하지 않으면 `commit`, `push`, PR 생성은 하지 않는다.

## 개발 환경 역할

- **데스크탑 PC가 주 개발·실행 환경이다.** 대용량 데이터, 모델 학습, 전체 백테스트, 장시간 수집, 실시간 추론과 대시보드의 최종 로컬 검증은 데스크탑을 기준으로 한다.
- **노트북은 보조 개발 환경이다.** 코드 리뷰, 문서화, 작은 수정, synthetic 테스트, GitHub/Codex/Work 기반 분석과 다음 작업 준비를 우선한다.
- GitHub는 두 기기가 공유하는 소스코드·문서의 기준점이다. 한 기기에서 이어서 작업하기 전 최신 변경을 pull하고, 같은 브랜치에 서로 다른 uncommitted 작업을 동시에 만들지 않는다.
- Cloud/Work는 GitHub에 반영된 상태를 기준으로 판단한다. 데스크탑의 uncommitted 파일이나 Git에 없는 대용량 데이터·모델에 접근할 수 있다고 가정하지 않는다.
- 자세한 기기별 개발 및 동기화 규칙은 `docs/development-rules.md`를 따른다.

## 표현 규칙

- 이 프로젝트를 자동 매매 수익 보장 시스템처럼 설명하지 않는다.
- 뉴스는 매수/매도 신호가 아니라 변동성, 불확실성, 위험도 판단에 사용한다.
- 문서는 한국어로 작성하되 파일명, 경로명, 명령어, 옵션명, 함수명은 영어 그대로 유지한다.
